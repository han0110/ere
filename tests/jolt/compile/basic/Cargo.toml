[package]
edition = "2021"
name = "guest"
version = "0.1.0"

[features]
guest = []

[dependencies]
jolt = { package = "jolt-sdk", git = "https://github.com/a16z/jolt", tag = "v0.1.0-alpha" }

[workspace]
