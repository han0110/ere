name: Test and clippy OpenVM

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  test:
    uses: ./.github/workflows/test-zkvm.yml
    permissions:
      contents: read
      packages: write
    with:
      zkvm: openvm
      toolchain: 1.86.0
      test_ere_dockerized: true
      test_options: ''
